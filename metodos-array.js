/**
 * Los métodos de array son unos métodos de los arrays que nos permiten realizar operaciones tediosas de manera más snecilla
 */

let peces = ["Dory", "Nemo", "Burbujitas"];

function mostrarPezPorConsola(pez) {
  console.log(pez);
}

peces.forEach(mostrarPezPorConsola);
